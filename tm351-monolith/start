#!/bin/bash

service postgresql restart

mongod --fork --logpath /dev/stdout --dbpath ${MONGO_DB_PATH}

# Test dir
#if [ -d "$DIR" ]; then
#fi

# Test file
#if [ -f "$FILE" ]; then
#fi

if [ ! -d "/home/jovyan/content" ]; then
    mkdir -p /home/jovyan/content
    cp –r /var/dist/content/* /home/jovyan/content/
fi

fix-permissions $HOME

exec "$@" 
